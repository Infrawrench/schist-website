{
  "private": true,
  "scripts": {
    "build": "bun build.ts",
    "dev": "bun run build && wrangler dev",
    "deploy": "bun run build && wrangler deploy"
  },
  "devDependencies": {
    "cssnano": "^9.0.1",
    "postcss": "^8.5.26",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "uglify-js": "^3.19.3",
    "wrangler": "^4.127.1"
  }
}
